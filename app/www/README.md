# This a generated directory

In order to add contents to the directory, you should build the front for the app by using the following command in `<...>/betting_app/front`:

```console
npm run build-for-app
```
